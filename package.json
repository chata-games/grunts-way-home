{
  "name": "grunts-way-home",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A Czech language detective adventure.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check game.js && node --check src/content.js && node --check src/engine.js && node --test tests/*.test.js",
    "start": "python3 -m http.server 4173"
  }
}
